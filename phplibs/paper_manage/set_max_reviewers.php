<?php
	//Fulfills Editor's task
	//Designate maximum papers reviewers can each review
	setMaxReviewers($conf_id, $max){
		//$query = set the paper's reviewers_max value with $max, find it by conf_id
		//$setMax = new DBQuery($query);
		//$setMax->execute_query();
		//$setMax->close();
	}
?>