<?php
	require_once('DBQuery.php');
	//Insert new paper information into paper table
	insertPaper($title,$reference,$speaker_id,$conf_id){
		//$query = insert new paper with $title, $reference, $speaker_id, and $conf_id in paper table,
		//id is incremental, the rest of the columns is NULL
		//$insert = new DBQuery($query);
		//$insert->execute_query();
	}
?>